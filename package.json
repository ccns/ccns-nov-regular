{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:goerli": "hardhat --network goerli run scripts/deploy.js",
    "mint:goerli": "hardhat --network goerli run scripts/mint.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.3"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "dotenv": "^10.0.0",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.1",
    "hardhat": "^2.6.8",
    "mocha": "^9.1.3"
  }
}
